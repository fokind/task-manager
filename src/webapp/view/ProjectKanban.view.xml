<View controllerName="fokind.kanban.controller.ProjectKanban" 
	xmlns:mvc="sap.ui.core.mvc" 
	xmlns:grid="sap.ui.layout.cssgrid" 
	xmlns:dnd="sap.ui.core.dnd" 
	xmlns="sap.m" displayBlock="true">
	<Page title="{title}" showNavButton="true" navButtonPress="onBackPress">
		<headerContent>
			<Button icon="sap-icon://add" press="onAddPress" />
		</headerContent>
		<grid:CSSGrid gridTemplateColumns="repeat(3, 1fr)" items="{
				path: 'Tasks',
				templateShareable: true
			}">
			<Text text="{title}"/>
			<!-- <List items="{
					path: 'Tasks',
					templateShareable: false
				}">
				<headerToolbar>
					<Toolbar>
						<Title text="{title}"/>
					</Toolbar>
				</headerToolbar>
				<dragDropConfig>
					<dnd:DragInfo groupName="taskList" sourceAggregation="items"/>
					<dnd:DropInfo groupName="taskList" targetAggregation="items" dropPosition="Between" drop="onDrop"/>
					<dnd:DragDropInfo sourceAggregation="items" targetAggregation="items" dropPosition="Between" drop="onDrop"/>
				</dragDropConfig>
				<StandardListItem title="{title}" type="Active" press="onDetailPress"/>
			</List> -->
		</grid:CSSGrid>
	</Page>
</View>

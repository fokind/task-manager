<mvc:View controllerName="fokind.kanban.controller.Kanban"
    xmlns:mvc="sap.ui.core.mvc" xmlns:grid="sap.ui.layout.cssgrid"
	xmlns:dnd="sap.ui.core.dnd" xmlns="sap.m"
	displayBlock="true">
		<Page title="Kanban"
		showNavButton="true" 
    navButtonPress="onBackPress" >
			<grid:CSSGrid gridTemplateColumns="repeat(3, 1fr)">
				<List items="{odata>ToDo}">
					<headerToolbar>
						<Toolbar>
							<Title text="To do"/>
						</Toolbar>
					</headerToolbar>
					<dragDropConfig>
						<dnd:DragInfo groupName="taskList" sourceAggregation="items"/>
						<dnd:DropInfo groupName="taskList" targetAggregation="items" dropPosition="Between" drop="onDrop"/>
						<dnd:DragDropInfo sourceAggregation="items" targetAggregation="items" dropPosition="Between" drop="onDrop"/>
					</dragDropConfig>
					<StandardListItem title="{odata>summary}" type="Active" press="onDetailPress"/>
				</List>
			</grid:CSSGrid>
		</Page>
</mvc:View>